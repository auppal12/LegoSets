<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Set</title>
    <link rel="stylesheet" href="/css/main.css" />

</head>
<body tabindex="-1">
    <%- include('partials/navbar', {page: '/lego/addSet'}) %>
    
    <div class="container mx-auto p-7">
      <div class="hero max-h-fit rounded-md"
          style="background-image: url(https://wallpaperaccess.com/full/754632.jpg);">
          <div class="hero-overlay bg-opacity-60"></div>
          <div class="hero-content text-center text-neutral-content">
            <div class="max-w-md">
              <h1 class="mb-5 text-4xl font-bold">Add Set</h1>
              <p class="mb-5">Add a new Lego Set of your choice to the Collection</p>
            </div>
          </div>
        </div>
    <br />

    <form action="/lego/addSet" method="post">
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 gap-y-0">
            <div>
              <div class="mb-6">
                <label for="name" class="block text-gray-300 font-medium">Name</label>
                <input type="text" id="name" name="name" required
                  class="w-full border border-gray-300 rounded-md px-3 py-2 focus:border-blue-300">
              </div>
              <div class="mb-6">
                <label for="year" class="block text-gray-300 font-medium">Year</label>
                <input type="number" id="year" name="year" required
                  class="w-full border border-gray-300 rounded-md px-3 py-2 focus:border-blue-300">
              </div>
            </div>
            <div>
  
              <div class="mb-6">
                <label for="num_parts" class="block text-gray-300 font-medium">Number of Parts</label>
                <input type="number" id="num_parts" name="num_parts" required
                  class="w-full border border-gray-300 rounded-md px-3 py-2 focus:border-blue-300">
              </div>
              <div class="mb-6">
                <label for="img_url" class="block text-gray-300 font-medium">Image URL</label>
                <input type="url" id="img_url" name="img_url" required
                  class="w-full border border-gray-300 rounded-md px-3 py-2 focus:border-blue-300">
              </div>
            </div>
  
            <div>
              <div class="mb-6">
                <label for="theme_id" class="block text-gray-300 font-medium">Theme</label>
                <select id="theme_id" name="theme_id" required
                  class="w-full border border-gray-300 rounded-md px-3 py-2 focus:border-blue-300">              
                  <% themes.forEach(theme=>{ %>
                    <option value="<%= theme.id %>">
                    <%= theme.name %>
                    </option>
                    <% }) %>           
                </select>
              </div>
  
              <div class="mb-6">
                <label for="set_num" class="block text-gray-300 font-medium">Set Num</label>
                <input type="text" id="set_num" name="set_num" required
                  class="w-full border border-gray-300 rounded-md px-3 py-2 focus:border-blue-300">
              </div>
            </div>
          </div>
  
          <div class="mt-6 text-center">
            <button type="submit" class="btn btn-primary">Add Set</button>
          </div>
        </form>
      </div>
</body>
</html>
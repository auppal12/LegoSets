<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User History</title>
    <link rel="stylesheet" href="/css/main.css" />
</head>
<body>
    <div>
        <%- include('partials/navbar', {page:''})%>
            <div class="container mx-auto p-4">
                <div class="hero bg-base-200 rounded-lg p-6">
                    <div class="hero-content text-center ">
                        <div class="max-w-md">
                            <h1 class="text-5xl font-bold">Welcome, <%= session.user.userName %></h1>
                            <p class="py-6 font-bold">Email: <%= session.user.email %></p>
                        </div>
                    </div>
                </div>

                <br>

                <table class="table table-zebra">
                    <thead class="font-bold">
                        <tr>
                            <th>Login Date/Time</th>
                            <th>Client Information</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% session.user.loginHistory.forEach((historyItem)=> { %>
                            <tr>
                                <td>
                                    <%= new Date(historyItem.dateTime).toLocaleString() %>
                                </td>
                                <td>
                                    <%= historyItem.userAgent %>
                                </td>
                            </tr>
                            <% }); %>
                    </tbody>
                </table>
            </div>
</body>
</html>